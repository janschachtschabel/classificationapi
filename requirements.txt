# Generated from pyproject.toml for Vercel deployment
# Core FastAPI dependencies (minimal for serverless)
fastapi==0.104.1
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP client
httpx==0.25.2

# AI integration
openai==1.3.7

# Logging
loguru==0.7.2

# Environment variables
python-dotenv==1.0.0

# Serverless adapter
mangum==0.17.0

# Optional: Only if needed for full functionality
# uvicorn[standard]==0.24.0  # Not needed in serverless
# rdflib==7.0.0              # Heavy dependency
# cachetools==5.3.2          # Not persistent in serverless
# PyYAML==6.0.1              # Only if YAML config needed
# sentence-transformers==2.2.2  # Very large (500MB+), causes timeouts

# Additional dependencies that might be needed
python-multipart>=0.0.6
tenacity>=8.2.0
